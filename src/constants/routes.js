export const ROUTES = {
  HOME_PATH: "/",
  GUEST_PATH: "guest",
  NEW_GAME_PATH: "/new-game",
  ROOM_PATH: "room/:id",
  GOOGLE_USER_PATH: "/user/:id",
}

export const API_ROUTES = {
  ROOM_PATH: "/room",
}
